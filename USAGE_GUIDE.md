# دليل الاستخدام - منصة المنتجات الرقمية

## 🎉 المنصة جاهزة للاستخدام!

### 📊 حالة النظام
- ✅ Frontend: يعمل على http://localhost:5000
- ✅ Backend API: يعمل على http://localhost:3001
- ✅ قاعدة البيانات: PostgreSQL متصلة وجاهزة
- ✅ جميع الأنظمة: تعمل بكفاءة

---

## 🔐 حسابات تجريبية جاهزة

### حساب البائع
- **البريد الإلكتروني:** seller@test.com
- **كلمة المرور:** Test123456
- **النوع:** بائع (Seller)

### حساب المشتري
- **البريد الإلكتروني:** buyer@test.com
- **كلمة المرور:** Test123456
- **النوع:** مشتري (Buyer)

---

## 🚀 كيفية استخدام المنصة

### 1️⃣ تسجيل الدخول
1. افتح المنصة على: http://localhost:5000
2. اضغط على "تسجيل الدخول"
3. استخدم أحد الحسابات التجريبية أعلاه

### 2️⃣ كبائع - رفع مشروع
1. سجل دخول بحساب البائع
2. اذهب إلى "إضافة مشروع"
3. املأ تفاصيل المشروع:
   - العنوان
   - الوصف
   - السعر
   - التقنيات المستخدمة
   - رابط العرض التوضيحي
4. اضغط "إنشاء المشروع"

### 3️⃣ كمشتري - شراء مشروع
1. سجل دخول بحساب المشتري
2. تصفح المشاريع المتاحة
3. اضغط على مشروع لعرض التفاصيل
4. اضغط "شراء المشروع"
5. اختر طريقة الدفع
6. أكمل عملية الشراء

### 4️⃣ نظام Escrow (الضمان)
- عند الشراء، يتم حجز المبلغ في Escrow بشكل آمن
- **خصم تلقائي 15%** للمنصة من كل عملية
- فترة مراجعة **7 أيام** للمشتري
- بعد الموافقة، يتم تحويل 85% للبائع و15% للمنصة

### 5️⃣ الإشعارات والدردشة
- ستصلك إشعارات عند:
  - شراء مشروعك (للبائع)
  - تأكيد الشراء (للمشتري)
  - إطلاق الأموال
  - الرسائل الجديدة
- استخدم نظام الدردشة للتواصل بين البائع والمشتري

---

## 💰 نظام العمولة (15%)

### مثال حسابي:
```
سعر المشروع: 5000 ريال
────────────────────────
عمولة المنصة (15%):  750 ريال
مبلغ البائع (85%):    4250 ريال
────────────────────────
إجمالي:               5000 ريال
```

---

## 📱 الصفحات المتاحة

### للجميع
- `/` - الصفحة الرئيسية
- `/projects` - تصفح المشاريع
- `/projects/[id]` - تفاصيل المشروع
- `/login` - تسجيل الدخول
- `/register` - إنشاء حساب جديد
- `/contact` - اتصل بنا

### للبائعين
- `/profile/seller` - الملف الشخصي
- `/seller/add-project` - إضافة مشروع
- `/projects/seller/my-projects` - مشاريعي

### للمشترين
- `/profile/buyer` - الملف الشخصي
- `/checkout/[projectId]` - إتمام الشراء
- `/checkout/escrow` - Escrow checkout
- `/transactions` - المعاملات
- `/transactions/[id]` - تفاصيل المعاملة

### للجميع
- `/chat` - الدردشة
- `/notifications` - الإشعارات

---

## 🔧 API Endpoints المتاحة

### المصادقة
```bash
POST /api/auth/register/seller      # تسجيل بائع
POST /api/auth/register/customer    # تسجيل مشتري
POST /api/auth/login                # تسجيل دخول
GET  /api/auth/profile              # الملف الشخصي
PUT  /api/auth/profile              # تحديث الملف
```

### المشاريع
```bash
GET  /api/projects                  # جميع المشاريع
GET  /api/projects/:id              # تفاصيل مشروع
POST /api/projects                  # إنشاء مشروع (بائع)
PUT  /api/projects/:id              # تحديث مشروع (بائع)
DELETE /api/projects/:id            # حذف مشروع (بائع)
```

### المعاملات
```bash
POST /api/transactions              # إنشاء معاملة (مشتري)
GET  /api/transactions              # معاملات المستخدم
GET  /api/transactions/:id          # تفاصيل معاملة
POST /api/transactions/:id/release  # إطلاق Escrow
POST /api/transactions/:id/refund   # طلب استرداد
```

### الإشعارات
```bash
GET /api/notifications              # جميع الإشعارات
GET /api/notifications/unread-count # عدد غير المقروءة
PATCH /api/notifications/:id/read   # تحديد كمقروء
DELETE /api/notifications/:id       # حذف إشعار
```

### الدردشة
```bash
POST /api/chat/send                 # إرسال رسالة
GET  /api/chat/conversations        # المحادثات
GET  /api/chat/:user_id             # رسائل مع مستخدم
```

---

## 🛡️ الأمان

تم تطبيق أفضل ممارسات الأمان:

1. ✅ **JWT Authentication** - مصادقة آمنة
2. ✅ **bcrypt** - تشفير كلمات المرور
3. ✅ **Role-based Access** - صلاحيات حسب نوع المستخدم
4. ✅ **CORS Protection** - حماية من الطلبات غير المصرح بها
5. ✅ **Input Validation** - التحقق من المدخلات
6. ✅ **SQL Injection Prevention** - حماية من SQL Injection
7. ✅ **Admin Verification** - التحقق من صلاحيات الإدارة من قاعدة البيانات

---

## 📊 قاعدة البيانات

### الجداول الموجودة:
1. **users** - المستخدمين (بائعين ومشترين)
2. **projects** - المشاريع المعروضة
3. **transactions** - المعاملات
4. **escrow** - حفظ الأموال
5. **installments** - الأقساط
6. **notifications** - الإشعارات
7. **messages** - الرسائل
8. **platform_earnings** - أرباح المنصة (15%)

---

## 🎯 الخطوات التالية (اختياري)

لتطوير المنصة أكثر، يمكنك:

1. **إضافة بوابات دفع حقيقية**
   - Stripe
   - PayPal
   - Tap Payments

2. **تحسينات إضافية**
   - نظام التقييمات والمراجعات
   - لوحة تحكم للإدارة
   - إحصائيات وتقارير
   - نظام اشتراكات للبائعين

3. **نشر المنصة**
   - استخدم زر "Deploy" في Replit
   - أو انشرها على Vercel / Railway

---

## 📞 الدعم

في حال واجهت أي مشاكل:

1. تحقق من سجلات الأخطاء في Console
2. تأكد من أن Backend و Frontend يعملان
3. راجع ملف `replit.md` للمزيد من التفاصيل

---

## ✨ ملاحظات مهمة

- ⚠️ المشاريع المرفوعة حالياً هي بيانات تجريبية
- 🔄 يمكنك حذف البيانات التجريبية وإضافة بيانات حقيقية
- 💾 جميع البيانات محفوظة في قاعدة بيانات PostgreSQL
- 🚀 المنصة جاهزة للاستخدام الفوري!

---

**تم بناء هذه المنصة بالكامل باستخدام:**
- Frontend: Next.js 14 + TypeScript + Tailwind CSS
- Backend: Express.js + Node.js
- Database: PostgreSQL (Neon)
- Authentication: JWT + bcrypt

**العمولة الافتراضية: 15%** (قابلة للتعديل في backend/.env)
